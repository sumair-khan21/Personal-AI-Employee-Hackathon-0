# AI Employee — Dependencies
# Install with: .venv/bin/pip install -r requirements.txt

# ── Bronze Tier ───────────────────────────────────────────────
# File system monitoring
watchdog>=4.0.0

# ── Silver Tier ───────────────────────────────────────────────
# Gmail API (OAuth2 + Google API client)
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.120.0

# Playwright Python package (browser automation for LinkedIn/Facebook/Instagram)
playwright>=1.58.0
# After installing, run once: .venv/bin/playwright install chromium

# ── Gold Tier ──────────────────────────────────────────────────
# Job scheduler for orchestrator.py
schedule>=1.2.0

# Image generation for Instagram text-image fallback (facebook/instagram watchers)
Pillow>=10.0.0
python-dotenv>=1.0.0

# ── Platinum Tier ──────────────────────────────────────────────
# Telegram bot for mobile notifications + HITL approval
python-telegram-bot>=20.7

# Web dashboard (FastAPI)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
jinja2>=3.1.0
